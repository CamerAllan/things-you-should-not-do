<script lang="ts">
	import Router from "./lib/PoorMansNav/PoorMansClientSideRouter.svelte";
	import Route from "./lib/PoorMansNav/PoorMansRoute.svelte";
	import Redirect from "./lib/Redirect.svelte";
	import Tysnd from "./lib/TYSND.svelte";

	async function getCatalogue() {
		const res = await fetch(`/data/catalogue.json`);
		const values = await res.json();

		return values;
	}

	let cataloguePromise = getCatalogue();
</script>

<main class="main">
	<div class="header"> 
		MORE UPDATES TO XKCD'S "THINGS YOU SHOULD NOT DO" LIST<br/>
		AS SUGGESTED BY <s>GPT-3</s> GPT-4
	</div>
	<div class="content">
		<Router>
			{#await cataloguePromise then catalogue}
				<Route path={"/"}>
					<Redirect path={`/part/${catalogue.latest}`} />
				</Route>
				<Route path={"/part/{part}"}>
					<Tysnd
						all_catalogue={catalogue.all}
						latestPart={catalogue.latest}
					/>
				</Route>
			{:catch error}
				{"Not ready yet! ;)"}
			{/await}
		</Router>
	</div>

	<div class="footer">
		Come back daily<a href="/part/7331">*</a> for updates :)
		<br />These lists are generated by OpenAI's models, and have not necessarily been reviewed
		by a human - opinions are not my own!<br />
		View the <a href="https://github.com/CamerAllan/things-you-should-not-do">source</a>. 
		<br />
		Based on <a href={"https://xkcd.com/2669/"}>this comic</a> by XKCD.
		<br/>
		Thanks to <a href="https://github.com/ipython/xkcd-font">xkcd-font</a> for the font!
		<br />
		Maintained by
		<a href="https://github.com/CamerAllan">Cameron Allan</a>.
		<br />
	</div>
</main>

<style>
	.main {
		display: flex;
		min-height: 100vh;
		flex-direction: column;
	}

	.header {
		margin-top: 20px;
		text-align: center;
	}

	.content {
		margin-top: 20px;
		flex: 1;
	}

	.footer {
		text-align: center;
		margin-bottom: 20px;
	}

	@font-face {
		font-family: "xkcd";
		src: url("/fonts/xkcd-script.ttf");
	}
</style>
